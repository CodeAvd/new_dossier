Round avatar with image or serif-italic initials fallback, optional accent ring + status dot.

```jsx
<Avatar src="/me.jpg" alt="Grigorii Avdeev" size="lg" ring status />
<Avatar initials="ga" size="md" />
```

Sizes `sm` / `md` / `lg` / `xl`. Falls back to `initials` when `src` is missing.
