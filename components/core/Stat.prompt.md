Evidence-first metric — big serif number, italic unit accent, mono label. The brand's "every claim has a number" motif.

```jsx
<Stat value="98.2" unit="%" label="citation recall" />
<Stat value="+1.8" label="sharpe" sub="live, 14 mo" size="lg" />
<Stat value="40" unit="ms" label="p50 latency" size="sm" />
```

Sizes `sm` / `md` / `lg`. Keep labels lowercase and terse.
